apiVersion: v1
kind: Service
metadata:
  name: new-one
  labels: new-one
spec:
  type: LoadBalancer
  selector:
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
      protocol: { { .Values.service.protocol } }
